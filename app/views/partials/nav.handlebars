 
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="">軟體保管單系統</a>
    </div>
    <ul class="nav navbar-nav">
       {{#if menu}}
  
            {{#each menu}}
                  
                  	<li><a href="{{Href}}">{{{Title}}}</a></li>
                
              {{/each}}
          {{/if}}
				
					
     
    </ul>
  </div>
</nav>
